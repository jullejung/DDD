<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="JoinMapper">

	<select id="join" parameterType="kr.co.DDD.util.dto.ProjectUserDTO"
						resultType="kr.co.DDD.util.dto.ProjectUserDTO">
	select user_no, user_id, user_pwd, user_name, phone, user_date;
	from user
	where user_id = #{user_id} and user_mpwd = #{user_mpwd}
	</select>

</mapper>

